version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.11"
  commands:
    - asdf plugin add uv
    - asdf install uv latest
    - asdf global uv latest
    - uv sync --extra docs
    - uv run sphinx-build -b html docs/ $READTHEDOCS_OUTPUT/html
